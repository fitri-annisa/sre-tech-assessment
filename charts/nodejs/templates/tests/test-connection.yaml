apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sre-tech-assessment-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sre-tech-assessment-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sre-tech-assessment-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
