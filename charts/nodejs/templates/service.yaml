apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-tech-assessment-chart.fullname" . }}
  labels:
    {{- include "sre-tech-assessment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sre-tech-assessment-chart.selectorLabels" . | nindent 4 }}
